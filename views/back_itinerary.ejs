<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta http-equiv="X-UA-Compatible" content="IE=edge">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="stylesheet" href="/css/bootstrap.css">
     <link rel="stylesheet" href="/css/myBackStyle.css">
     <title>Document</title>
</head>
<body>
     <header class="d-flex justify-content-start">
          <div>
               <%- include('share/back_nav.ejs')%> 
          </div>
          <div class="backBlock"></div>
          <div>
               <div style="height: 50px; width: 100%;"></div>
               <h2>行程管理</h2>
               <a href="/background/itineraryAdd" class="btn btn-info">新增行程</a>
               <hr>
               <table class="backtable mt-2">
                    <tr class="bg-info">
                         <th class="backCol">行程標題</th>
                         <th class="backCol-s">地區</th>
                         <th class="backCol">出發日期</th>
                         <th class="backCol">回程日期</th>
                         <th class="backCol-s">價格</th>
                         <th class="backCol">航空</th>
                         <th></th>
                         <th></th>
                    </tr>
                    <% for(var i=0;i < order.length; i++){ %>
                    <tr>
                         <td><%= order[i].Title %></td>
                         <td><%= order[i].GoTo %></td>
                         <td><%= order[i].GoDate %></td>
                         <td><%= order[i].ArrDate %></td>
                         <td><%= order[i].Price %></td>
                         <td><%= order[i].GoAir %></td>
                         <td><button class="btn btn-warning m-1">修改</button></td>
                         <td><button class="btn btn-danger m-1">刪除</button></td>
                    </tr>
                    <% } %>
               </table>
          </div>
     </header>

     
</body>
</html>